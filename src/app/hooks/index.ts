// Barrel for shared UI/data hooks used across the app.
// Feature-specific hooks live next to their feature (e.g. features/compare, features/cost-estimator).
export { usePagination } from "./usePagination";
export { useFilteredData } from "./useFilteredData";
export { useIsMobile } from "./useIsMobile";
export { useElementWidth } from "./useElementWidth";
export { useSearchResetOnNavigate } from "./useSearchResetOnNavigate";
